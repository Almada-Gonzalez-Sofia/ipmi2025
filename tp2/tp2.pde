String texto = 
float x, y; 
color relleno=


void setup(){ 
  size(640, 480);
  println( texto );
  x = width / 2;
  y = height / 2;
  relleno = color(200, 0, 0);
}
  
  
void draw(){ 
  fill( relleno );
  textSize( 20 );
  textAlign( CENTER, CENTER);
 
}
